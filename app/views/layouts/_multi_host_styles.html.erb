<% if session[:current_multi_host_name].present? && session[:current_multi_host_name] != 'unknown' && session[:current_multi_host_name] != 'default' %>
  <%= stylesheet_link_tag "multi_hosts/#{session[:current_multi_host_name]}" %>
<% end %>